const Sample = () => {
  return (
    <div>
      <button
        className="button"
        onClick={() => {
          console.log('button');
        }}
      ></button>
    </div>
  );
};

export default Sample;
